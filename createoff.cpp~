#include<iostream>
#include<fstream>
#include<cmath>
int roty[3][3] = {{1,0,0},{0,1,0},{0,0,1}};
using namespace std;
int main(){
	int vertices = 398, faces = 400, edges= 408;
	ofstream outfile;
	ifstream infile;
	outfile.open("SOR.off", ios::out);
	outfile<<"OFF"<<endl;
	outfile<<vertices<<" "<<faces<<" "<<edges<<endl;
	outfile.close();
	return 0;
}
